# Documentation Boundary

- `docs/` is public-safe content only (assume publishable).
- `docs-private/` is private ops content (ignored by git).
- Never include IPs, wallet file paths, private keys, RPC creds, or host-level runbooks in `docs/`.
